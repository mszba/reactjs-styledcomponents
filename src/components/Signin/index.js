import React from 'react';
import { SigninContainer } from './SigninElements';

const Signin = () => {
  return (
    <SigninContainer>
      <h1>Sign in page</h1>
    </SigninContainer>
  );
};

export default Signin;
